<h3>User Submissions</h3>

<table width="100%">
  <tr>
    <th>Submission</th>
    <th><%= @turkee_task.allow_voting? ? "# Votes" : "" %></th>
  </tr>

  <% @iterations.each do |iteration| %>
      <tr>
        <td><%= simple_format iteration.value %></td>
        <td><%= iteration.iteration_votes.count if @turkee_task.allow_voting? %></td>
      </tr>
  <% end %>
</table>
