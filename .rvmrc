
if [[ -d "${rvm_path:-$HOME/.rvm}/environments" \
  && -s "${rvm_path:-$HOME/.rvm}/environments/ruby-1.9.2-p180@turkee_iterator_31" ]] ; then
  \. "${rvm_path:-$HOME/.rvm}/environments/ruby-1.9.2-p180@turkee_iterator_31"
else
  rvm --create  "ruby-1.9.2-p180@turkee_iterator_31"
fi
